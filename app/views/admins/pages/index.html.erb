<h1>Listing admins_pages</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @admins_pages.each do |admins_page| %>
      <tr>
        <td><%= link_to 'Show', admins_page %></td>
        <td><%= link_to 'Edit', edit_admins_page_path(admins_page) %></td>
        <td><%= link_to 'Destroy', admins_page, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Page', new_admins_page_path %>
